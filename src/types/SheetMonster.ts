type SheetMonster = {
    monster_id: number;
    name_en: string;
    planId: string;
    requiredMemento: string;
    matchFever: number[];
    matchSusFever: number[];
    matchSusNormal: number[];
};

export default SheetMonster;